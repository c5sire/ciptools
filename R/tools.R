

new_description <- function() {
  create_description(extra=list("Version: 0.0.1.9000"))
  
}


# Assume a new R project has been created including git and user is under this working directory
#
# Check if files present (including git dir); if not create them!
# Make adjustements to DESCRIPTION file
#
# etc
# when finished make a commit to git and try to push to git repository. Check for internet
# connectivity, check for presence of repository on github 
# https://developer.github.com/v3/repos/#list-your-repositories
#
# GET /users/:username/repos
# POST /user/repos
# 
# and push.
#
# Remaining things to do for user: register on three additional sites and manage package
#

add_cip <- function(params <- list(author="Reinhard Simon", email="R.SIMON@CGIAR.ORG")){
  
}

